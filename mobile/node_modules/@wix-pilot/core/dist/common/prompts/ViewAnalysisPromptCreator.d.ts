import { TestingFrameworkAPICatalog, PreviousStep } from "../../types";
export declare class ViewAnalysisPromptCreator {
    private apiCatalog;
    constructor(apiCatalog: TestingFrameworkAPICatalog);
    createPrompt(step: string, viewHierarchy: string, previousSteps?: PreviousStep[]): string;
}
